import { ArrowUpRight, Bookmark, Clock3 } from 'lucide-react';
import { Link } from 'wouter';
import { IMAGES, JOURNAL_POSTS } from '@/lib/data';
import { PageFrame, PageIntro } from '@/components/SiteChrome';

export default function Journal() {
  return <PageFrame><PageIntro kicker="The journal / 04" title={<>Notes from<br /><em>the universe.</em></>} body={<p>Field notes, studio visits, collecting rituals, and other things we think are worth keeping around.</p>} color="yellow" image={IMAGES[0].src} badge="Field Notes / Vol. 26" /><main className="journal-page"><div className="container"><section className="journal-feature"><div className="journal-feature__image"><img src={JOURNAL_POSTS[0].image} alt="A colorful studio table" /><span>Editor's pick</span></div><div className="journal-feature__copy"><p className="eyebrow">{JOURNAL_POSTS[0].category} / {JOURNAL_POSTS[0].date}</p><h2>{JOURNAL_POSTS[0].title}</h2><p>{JOURNAL_POSTS[0].excerpt}</p><div className="journal-meta"><span><Clock3 size={15} /> 4 min read</span><button aria-label="Save article"><Bookmark size={17} /></button></div><Link href="/journal" className="button button--dark">Read the story <ArrowUpRight size={17} /></Link></div></section><div className="journal-list-heading"><p className="eyebrow">More from the journal</p><span>03 stories / updated monthly</span></div><section className="journal-list">{JOURNAL_POSTS.slice(1).map((post, index) => <article className="journal-list__item" key={post.id}><div className="journal-list__number">0{index + 2}</div><div className="journal-list__image"><img src={post.image} alt="" /></div><div className="journal-list__copy"><p className="eyebrow">{post.category} / {post.date}</p><h3>{post.title}</h3><p>{post.excerpt}</p><Link href="/journal" className="text-link">Read note <ArrowUpRight size={15} /></Link></div></article>)}</section></div><section className="journal-quote"><p>“A collection is just a diary with better lighting.”</p><span className="eyebrow">TOONHUB BY AADI FIELD NOTE 014</span></section></main></PageFrame>;
}
